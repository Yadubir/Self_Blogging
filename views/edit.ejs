<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full relative flex flex-col gap-5 bg-zinc-900 h-screen">
        <div class="mt-3 text-white text-sm">
            <a class="bg-red-500 text-white rounded-md px-2 py-1 mt-4 ml-2" href="/post"> back to your post</a>
        </div>
        <h3 class="text-gray-300 ml-12 mt-5">Edit your post</h3>
        <div class="w-full relative mt-5 p-10 ">
            <form action="/update/<%= post._id %>" method="post">
                <div class="relative mt-3">
                    <textarea
                        class="block w-full max-w-xs h-40 px-4 py-2 text-sm font-normal shadow-xs text-gray-300 bg-transparent border border-gray-300 rounded-lg placeholder-gray-400 focus:outline-none leading-relaxed resize-none"
                        placeholder="" name="content"> <%= post.content %> </textarea>
                </div>
                <input class="bg-green-500 text-white rounded-md px-2 py-1 mt-4" type="submit" value="Update post">
            </form>
        </div>
    </div>
</body>

</html>