<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full relative flex flex-col gap-5 bg-zinc-900 h-screen">
        <div class="mt-3 text-white text-sm">
            <a class="bg-red-500 text-white rounded-md px-2 py-1 mt-4 ml-2" href="/profile"> back to profile</a>
        </div>
        <h3 class="text-gray-300 ml-12 mt-5">Create a post</h3>
        <div class="w-full relative mt-5 p-10 ">
            <form action="/post" method="post">
                <div class="relative">
                    <input type="text" id="default-search"
                        class="block w-full max-w-xs px-4 py-2 text-sm font-normal shadow-xs text-gray-300 bg-transparent border border-gray-300 rounded-lg placeholder-gray-400 focus:outline-none leading-relaxed "
                        placeholder="@<%= user.username %>" name="username">
                </div>
                <div class="relative mt-3">
                    <textarea
                        class="block w-full max-w-xs h-40 px-4 py-2 text-sm font-normal shadow-xs text-gray-300 bg-transparent border border-gray-300 rounded-lg placeholder-gray-400 focus:outline-none leading-relaxed resize-none"
                        placeholder="What's on your mind..." name="content"></textarea>
                </div>
                <input class="bg-green-500 text-white rounded-md px-2 py-1 mt-4" type="submit" value="Create post">
            </form>
        </div>
        <div class="posts ml-12">
            <h3 class="text-gray-400">Your Posts.</h3>
            <div class="flex mt-3 flex-wrap">
                <div class="postcontainer">
                    <div class="flex">
                        <% user.posts.reverse().forEach(function(post){ %>
                            <div class="w-full max-w-xs h-40 px-4 py-2 text-sm font-normal shadow-xs text-gray-300 bg-transparent border border-gray-300 rounded-lg placeholder-gray-400 focus:outline-none leading-relaxed resize-none mr-2 mb-2">
                                <h4 class="text-blue-600">@<%= user.username %></h4>
                                <p class="tracking-tight mt-5"> <%= post.content %></p>
                                <div class="btns flex mt-3 gap-4">
                                    <a class="text-blue-600" href="">Like</a>
                                    <a class="text-gray-400" href="/edit/<%= post._id %>">Edit</a>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>